# Prompt user for input.
user_input = input("Enter a sentence: ")

# Find length of input using len() method (Including spaces).
char_count = len(user_input)

# Print the number of characters in the input.
print(char_count)